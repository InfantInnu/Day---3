// For the given JSON iterate over all for loops (for, for in, for of, forEach)

// for loop: for - loops through a block of code a number of times
// syntax- for(initialisation;condition;increment or decrement){}
// Example: for (let i = 0; i < 5; i++) {
//   
//  }

// for in loop: for/in - loops through the properties of an object, forward iteration(one way)
// syntax- for(key in object){}
// Example 1: for(var i in studentname){}
// Example 2:const person = {fname:"John", lname:"Doe", age:25};
//                    let text = "";
//                    for (let x in person) {
//                     text += person[x];
//                           }


// for of: The for...of statement creates a loop iterating over iterable objects, including: built-in String, Array, array-like objects (e.g., arguments or NodeList), 
//          TypedArray, Map, Set, and user-defined iterables.
// syntax-for (variable of iterable) {
//           statement
//            }
// Example: const array1 = ['a', 'b', 'c'];

//              for (const element of array1) {
//                console.log(element);
//                  }

// for Each: The forEach () loop  it executes the given function once for each element in an array in ascending order. It doesn't execute the callback function for empty array elements.
// syntax- forEach((element, index, array) => { /* ... */ })
// Example: const array1 = ['a', 'b', 'c'];
 //          array1.forEach(element => console.log(element));


  
    


